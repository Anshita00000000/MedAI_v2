# ── Core web framework ────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3
streamlit==1.45.1

# ── ASR ───────────────────────────────────────────────────────────────────────
openai-whisper==20240930

# ── ML frameworks ─────────────────────────────────────────────────────────────
# torch / transformers / accelerate are pre-installed on Colab and GPU VMs.
# List them here only for plain venv installs; Colab users skip these.
torch>=2.2.0,<3.0.0
transformers==4.51.3
accelerate==1.6.0

# ── Speaker diarisation ───────────────────────────────────────────────────────
pyannote.audio==3.3.2

# ── NLP / NER ─────────────────────────────────────────────────────────────────
spacy==3.7.5
scispacy==0.5.5
medspacy==1.1.3
# negspacy removed — medspacy provides negation natively
# gliner is optional (zero-shot NER); install separately if needed:
#   pip install gliner==0.2.18

# ── Vector store / RAG ────────────────────────────────────────────────────────
sentence-transformers==3.4.1
chromadb==0.6.3

# ── PDF / reporting ───────────────────────────────────────────────────────────
weasyprint==62.3
jinja2==3.1.6

# ── Evaluation ────────────────────────────────────────────────────────────────
jiwer==3.0.5
rouge-score==0.1.2
bert-score==0.3.13

# ── Data / utilities ──────────────────────────────────────────────────────────
pandas==2.2.3
# numpy: accept both 1.x and 2.x — do NOT pin to 1.26.x on envs where
# torch 2.x has already pulled in numpy 2.x
numpy>=1.26.0,<3.0.0
requests==2.32.3
beautifulsoup4==4.13.4
python-dotenv==1.1.0
pydantic==2.11.4
pypdf==5.5.0

# ── Google Gemini (new SDK — NOT google-generativeai) ─────────────────────────
google-genai==1.14.0
